import * as THREE from 'three';

export const Scene3DSettings = {
  canvas: `animation-screen`,
  color: new THREE.Color(`rgb(76, 49, 121)`),
  alpha: 1,
  far: 5500,
  fov: 35,
  near: 1,
  cameraPozitionZ: 5,
  cameraPozitionY: 0,
};

export const TIMER_DURATION_MIN = 5;
